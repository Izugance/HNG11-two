const { userRouter } = require("./user.js");
const { authRouter } = require("./auth.js");
const { organisationRouter } = require("./organisation.js");

module.exports = { userRouter, authRouter, organisationRouter };
